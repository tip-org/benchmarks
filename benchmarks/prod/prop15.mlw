module A
  use HighOrd
  type nat = Z | S nat
  function plus (ds : nat) (y : nat) : nat =
    match ds with
    | Z -> y
    | S x -> S (plus x y)
    end
  goal prop_T15 : forall x2 : nat . plus x2 (S x2) = S (plus x2 x2)
end

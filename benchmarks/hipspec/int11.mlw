module A
  use HighOrd
  type sign = Pos | Neg
  function opposite (ds : sign) : sign =
    match ds with
    | Pos -> Neg
    | Neg -> Pos
    end
  goal prop_11 : forall s : sign . opposite (opposite s) = s
end

module A
  use HighOrd
  type nat = Z | S nat
  type list 'a = Nil | Cons 'a (list 'a)
  function append (ds : list 'a2) (ys : list 'a2) : list 'a2 =
    match ds with
    | Nil -> ys
    | Cons x xs -> Cons x (append xs ys)
    end
  function le (ds2 : nat) (ds1 : nat) : bool =
    match ds2 with
    | Z -> True
    | S ipv ->
        match ds1 with
        | Z -> False
        | S ipv1 -> le ipv ipv1
        end
    end
  function equal2 (ds3 : nat) (ds12 : nat) : bool =
    match ds3 with
    | Z ->
        match ds12 with
        | Z -> True
        | S ipv2 -> False
        end
    | S ds22 ->
        match ds12 with
        | Z -> False
        | S y -> equal2 ds22 y
        end
    end
  function count (x2 : nat) (ds4 : list nat) : nat =
    match ds4 with
    | Nil -> Z
    | Cons y2 ys2 ->
        match equal2 x2 y2 with
        | False -> count x2 ys2
        | True -> S (count x2 ys2)
        end
    end
  goal prop_03 :
    forall n : nat, xs2 : list nat, ys3 : list nat .
      le (count n xs2) (count n (append xs2 ys3)) = True
end

module A
  use HighOrd
  type nat = Z | S nat
  function minus (ds : nat) (ds1 : nat) : nat =
    match ds with
    | Z -> Z
    | S ipv ->
        match ds1 with
        | Z -> S ipv
        | S ipv1 -> minus ipv ipv1
        end
    end
  goal prop_10 : forall m : nat . minus m m = Z
end

module A
  use HighOrd
  type nat = Z | S nat
  function min (ds : nat) (y : nat) : nat =
    match ds with
    | Z -> Z
    | S x ->
        match y with
        | Z -> Z
        | S y1 -> S (min x y1)
        end
    end
  goal prop_32 : forall a : nat, b : nat . min a b = min b a
end

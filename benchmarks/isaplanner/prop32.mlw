module A
  use HighOrd
  type nat = S nat | Z
  function min (ds : nat) (y : nat) : nat =
    match ds with
    | S x ->
        match y with
        | S y1 -> S (min x y1)
        | Z -> Z
        end
    | Z -> Z
    end
  goal prop_32 : forall a : nat, b : nat . min a b = min b a
end

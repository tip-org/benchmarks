(* Source: IsaPlanner test suite *)
module A
  use HighOrd
  type nat = Z | S nat
  function max (ds : nat) (y : nat) : nat =
    match ds with
    | Z -> y
    | S ipv ->
        match y with
        | Z -> ds
        | S ipv1 -> S (max ipv ipv1)
        end
    end
  goal prop_23 : forall a : nat, b : nat . max a b = max b a
end
